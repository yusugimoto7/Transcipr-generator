export const metadata = {
  title: "Sugimoto Draws",
  description: "Canadian immigration draw watcher and auto-poster.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
